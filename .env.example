DATABASE_URL=postgresql://tamilsh:pocpassword@localhost:5432/tamilsh_poc

# Required to run tests. Must always point at an isolated *_test database.
# Tests fail closed (refuse to run) if this is unset, empty, or points at
# the dev database above.
TEST_DATABASE_URL=postgresql://tamilsh:pocpassword@localhost:5433/tamilsh_poc_test
